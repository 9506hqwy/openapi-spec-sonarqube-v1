content:
  application/json:
    schema:
      properties:
        plugins:
          items:
            properties:
              requiredForLanguages:
                items:
                  type: string
