content:
  application/json:
    schema:
      properties:
        Health Causes:
          items:
            type: string
        Plugins:
          properties:
            name:
              type: string
