content:
  application/json:
    schema:
      properties:
        qualitygates:
          items:
            properties:
              caycStatus:
                type: string
        default:
          type: string
