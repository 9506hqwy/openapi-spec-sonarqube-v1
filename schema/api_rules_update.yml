content:
  application/json:
    schema:
      properties:
        rule:
          properties:
            tags:
              items:
                type: string
            sysTags:
              items:
                type: string
