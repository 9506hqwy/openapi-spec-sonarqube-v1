content:
  application/json:
    schema:
      properties:
        user:
          properties:
            groups:
              items:
                type: string
            scmAccounts:
              items:
                type: string
