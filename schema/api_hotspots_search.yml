content:
  application/json:
    schema:
      properties:
        hotspots:
          items:
            properties:
              messageFormattings:
                items:
                  type: string
              flows:
                items:
                  type: string
